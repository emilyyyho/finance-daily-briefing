# 定制化财经日报配置。敏感信息只放在 GitHub Actions Secrets。
lookback_hours: 36
max_news: 20
# 有足够中文财经热榜时，日报至少保留此数量；不足时自动使用其他来源补齐。
min_china_news: 8
max_filings_per_company: 8

# 仅在这里填写你真正关注的标的；留空时跳过 AKShare/EDGAR 标的查询。
watchlist:
  a_share: []
  # 示例：
  # a_share:
  #   - symbol: "600519"
  #     name: "贵州茅台"
  us:
    # 可填 ticker（脚本会从 SEC 官方公司目录自动解析 CIK），也可直接填 CIK。
    # 示例：
    # - cik: "320193"
    #   ticker: "AAPL"
    #   name: "Apple"
    []

ai:
  # 没有 GEMINI_API_KEY 时自动退化为规则整理，不会阻塞推送。
  enabled: true
  model: "gemini-2.5-flash"
  max_output_tokens: 1800
  temperature: 0.2
